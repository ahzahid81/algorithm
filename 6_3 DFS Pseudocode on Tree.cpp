/*
void dfs(int node)
{// binary
    cout<<node<<endl;

    //base case
    if node does not have any child;
        return
    dfs(node.left_child);
    dfs(node.right_child);


    // for all childs of node
    for all childs of node:
        dfs(child);
}
*/